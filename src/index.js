import * as home from './home'
import * as menuItems from './menu'

home.homepage()

// home.tab1.addEventListener('click', () => {
    
// });

home.tab2.addEventListener('click', () => {
    menuItems();
});

home.tab3.addEventListener('click', () => {
    
});
